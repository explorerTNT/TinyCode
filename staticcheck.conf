# Default staticcheck set (SA + selected ST/S/QF), minus two intentional cases:
#
# ST1005: tool error strings are surfaced verbatim as output to the model/user
#         (e.g. tools.ListFiles returns err.Error()), so capitalized, punctuated
#         sentences are deliberate.
# SA1019: go-openai deprecated MaxTokens in favour of MaxCompletionTokens, which
#         local llama.cpp / LM Studio backends do not accept yet.
checks = ["inherit", "-ST1005", "-SA1019"]
